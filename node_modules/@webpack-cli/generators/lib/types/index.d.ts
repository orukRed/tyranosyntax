import Generator from "yeoman-generator";
import { IWebpackCLI } from "webpack-cli";
export declare type InitOptions = {
    template: string;
    force?: boolean;
};
export declare type LoaderOptions = {
    template: string;
};
export declare type PluginOptions = {
    template: string;
};
export declare type InitGeneratorOptions = {
    generationPath: string;
} & InitOptions;
export declare type LoaderGeneratorOptions = {
    generationPath: string;
} & LoaderOptions;
export declare type PluginGeneratorOptions = {
    generationPath: string;
} & PluginOptions;
export declare type BaseCustomGeneratorOptions = {
    template: string;
    generationPath: string;
    force?: boolean;
};
export declare type CustomGeneratorOptions<T extends BaseCustomGeneratorOptions> = Generator.GeneratorOptions & {
    cli: IWebpackCLI;
    options: T;
};
export declare class CustomGenerator<T extends BaseCustomGeneratorOptions = BaseCustomGeneratorOptions, Z extends CustomGeneratorOptions<T> = CustomGeneratorOptions<T>> extends Generator<Z> {
    cli: IWebpackCLI;
    template: string;
    dependencies: string[];
    force: boolean;
    answers: Record<string, unknown>;
    generationPath: string;
    supportedTemplates: string[];
    packageManager: string | undefined;
    constructor(args: string | string[], opts: Z);
}
